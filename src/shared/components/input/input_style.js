import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  containerInput: {
    width: "100%",
    marginBottom: 10,
  },

  view: {
    height: 55,
  },

  inputView: {
    width: "100%",
    borderRadius: 15,
    backgroundColor: "white",
    borderColor: "#009e54",
    borderWidth: 1.5,
  },
});
